services:
  app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
      - DATABASE_URL=postgresql://postgres:password@db:5432/mydatabase
      - JWT_SECRET_KEY=your_secret_key
    volumes:
      - .:/app
    depends_on:
      - db

  db:
    image: postgres:13
    environment:
      POSTGRES_USER: quramdb3
      POSTGRES_PASSWORD: ChHsZ3VbMRfzlQLxJ91uod3Rc96bxqqy
      POSTGRES_DB: quramdb3_q4d1
    ports:
      - "5432:5432"
